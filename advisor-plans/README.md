# Implementation Plans (advisor)

Generated by the improve skill on 2026-07-17, at commit `64dfb98`. This folder
is `advisor-plans/` (not `plans/`) because the repo's own `Plans/` roadmap
corpus already exists and macOS is case-insensitive.

Execute in the order below unless dependencies say otherwise. Each executor:
read the plan fully before starting, honor its STOP conditions, and update your
row when done.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | One-command backend integration suite (`make test-integration`) | P1 | S | — | TODO |
| 002 | Retention sweep removes orphaned MinIO objects | P1 | S | 001 | TODO |
| 003 | 3MF uploads hash file bytes (fixes tee dedup + content addressing) | P1 | M | — (001 recommended) | DONE (`43ef87b`) — landed in the plan-02 completion sequence |
| 004 | Pin `"latest"` frontend deps to lockfile-resolved versions | P2 | S | — | TODO |
| 005 | Docs truth pass + root README/CLAUDE.md/.env.example | P2 | S | 001 (one sentence) | TODO |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale)

## Dependency notes

- 002 and (recommended) 003 verify through 001's `make test-integration` — land 001 first.
- 005 documents 001's new target; if 001 hasn't landed, 005 skips that sentence.
- 003 and 004 are independent of everything and of each other.

## Findings found but not planned (valid; selectable later)

From the 2026-07-17 audit — vetted but not selected in this round:

- **NaN/Inf metrics pass `validatePart`; `writeJSON` discards Encode errors** (`handlers.go:211,36-40`) — S effort.
- **No index on `quotes(email)`** for the /orders lookup (`quotes.sql:33-36`) — S; one migration.
- **Customer email logged in plaintext** (`handlers.go:361,541`) — S; GDPR surface, relevant to Plans/09.
- **Bambu Cloud token sits in `instant-quote/.env`** though only the backend consumes it — S; relocate + rotate.
- **`/orders` SSR hydration mismatch** — `getSessionEmail()` read during render (`$locale/orders.tsx:44`) — S.
- **Dead code**: `FactoryScene.tsx` (723 lines) + `FactoryHero.tsx` referenced only by each other — S deletion.
- **MakerWorld tee runs synchronously before the response write** (`handlers.go:551`) — S; move after/async.
- **`upload-file.ts` / `session.ts` / `funnel.ts` have no unit tests** — S.
- **No React component/route test layer at all** — L; Testing Library under `bun test`, start with quote submit.
- **`handlers.go` (645 lines) mixes DTO/validation/HTTP/persistence/tee** — M extraction.
- **Content-section routing duplicates a 4-way switch across two route files**; unknown sections silently render materials instead of 404 — M; registry-dispatch refactor, pin SEO head output with tests first.
- **Tracked binary artifacts** (.docx/.pptx/.xlsx at root, 588 KB .dc.html) — S relocation.

## Findings considered and rejected

- **Reduced-motion visitors download three.js hero chunk**: rejected — the analyzed `FactoryHero`/`FactoryScene` are dead code (live landing renders `Hero`, no three.js); the correct action is the deletion listed above.
- **Blog MDX eagerly bundles all posts**: not worth doing — 4 posts, and the code self-documents the split threshold (~20 posts).
- **nitro-nightly's pre-release transitive chain (rolldown/h3-rc/alpha)**: not actionable — no stable Nitro line supports TanStack Start yet; plan 004's pinning is the available mitigation. Revisit at Plans/03.
- **Per-part `GetFileByID` N+1 in `persistQuote`**: not worth doing — bounded by `MaxParts = 5`.
- **Upload confirm verifies size but not content hash**: not planned separately — folds into the already-deferred Go mesh-recompute follow-up (`Plans/02-file-storage.md`), which re-hashes stored bytes at order time.

## Direction options (maintainer's call, from the audit)

- **D1**: Finish plan 02's deferred half (Go mesh port + `VerifyOrderPricing`) — smallest unit that unblocks the revenue path; named prerequisite of plan 05.
- **D2**: Re-sequence 02-finish → 05 guest checkout (Stripe test mode) → 04 auth — earliest end-to-end paid-order proof; test-mode only until entity/Stripe/Fakturownia exist.
- **D3**: `GET /quotes/{id}` read-back — one query + one handler; prerequisite for quote links (plan 14) and abandoned-quote email (plan 06); scope response for PII.
- **D4**: STL-calculator linkable asset (`seo_prompts/06`) — all prerequisites shipped; SEO compounds only if started early; parallelizable.
